apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongodb
  namespace: roboshop
  # labels related to deployment resource
  labels:
    project: roboshop
    tier: db
    component: mongodb
    
spec:
  replicas: 1
  selector:
    matchLabels:
      project: roboshop
      tier: db
      component: mongodb
      
  # Pod
  template:
    metadata:
      labels:
        project: roboshop
        tier: db
        component: mongodb
        
    spec:
      containers:
      - name: mongodb
        image: venkey57/mongodb:4.0.0
---
apiVersion: v1
kind: Service
metadata:
  name: mongodb
  namespace: roboshop
  # servicer labels
  labels:
    project: roboshop
    tier: db
    component: mongodb
spec:
  # Service uses the below labels to select the pods
  selector:
    project: roboshop
    tier: db
    component: mongodb
  ports:
    - protocol: TCP
      port: 27017 # service port
      targetPort: 20717 # container port